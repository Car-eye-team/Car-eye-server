/*
 * 地图展示panel
 */
Ext.define('FMS.view.MapView', {
			extend : 'Ext.panel.Panel',
			alias : 'widget.mapView',
			layout:'fit',
			//height:Ext.getBody().getViewSize().height-121,
			id:'map',
			autoWidth:true
		});